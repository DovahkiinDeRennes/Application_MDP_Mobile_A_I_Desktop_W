CREATE TABLE user (
    id TEXT NOT NULL PRIMARY KEY,
    email TEXT NOT NULL UNIQUE,
    password TEXT NOT NULL,
    aes_key TEXT NOT NULL
);

insertUser:
INSERT INTO user(id, email, password, aes_key)
VALUES (?, ?, ?, ?);

getUserByEmail:
SELECT * FROM user WHERE email = ?;
